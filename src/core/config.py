# src/core/config.py

# =============================================================================
# 【说明】这些常量现在主要在 models.py 中定义
# 这里仅用于开发环境参考，打包环境以 models.py 为准
# =============================================================================

# 命令 ID 的正则表达式模式
# 含义：只允许小写字母、数字和下划线，且必须以字母开头
COMMAND_ID_PATTERN = r"^[a-z][a-z0-9_]*$"

# 自由模式下的最大状态嵌套层级限制
FREE_MODE_MAX_DEPTH = 5

# 系统事件历史的最大保留条数
MAX_EVENT_HISTORY_SIZE = 100

# =============================================================================
# 验证配置是否正确
# =============================================================================

if __name__ == "__main__":
    print(f"COMMAND_ID_PATTERN: {COMMAND_ID_PATTERN}")
    print(f"FREE_MODE_MAX_DEPTH: {FREE_MODE_MAX_DEPTH}")
    print(f"MAX_EVENT_HISTORY_SIZE: {MAX_EVENT_HISTORY_SIZE}")
